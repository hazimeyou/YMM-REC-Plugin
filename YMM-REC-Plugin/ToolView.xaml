<UserControl x:Class="YMM_REC_Plugin.ToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240"
             d:DesignWidth="420">
    <Grid Background="White" Margin="8">
        <StackPanel Margin="4">
            <TextBlock Text="録音デバイス" Margin="2,2,2,0" />
            <DockPanel Margin="2">
                <Button Content="再読込"
                        Command="{Binding RefreshDevicesCommand}"
                        Width="70"
                        Margin="8,0,0,0"
                        DockPanel.Dock="Right" />
                <ComboBox ItemsSource="{Binding AvailableDevices}"
                          SelectedItem="{Binding SelectedDevice}"
                          MinWidth="260" />
            </DockPanel>

            <TextBlock Text="録音仕様: WAV / 48000 Hz / 16 bit / Mono" Margin="2,10,2,0" />
            <TextBlock Text="{Binding RecordsDirectory, StringFormat=保存先: {0}}" TextWrapping="Wrap" Margin="2,4,2,0" />

            <StackPanel Orientation="Horizontal" Margin="2,12,2,0">
                <Button Content="録音開始"
                        Command="{Binding StartRecordingCommand}"
                        Width="120"
                        Margin="0,0,8,0" />
                <Button Content="録音停止"
                        Command="{Binding StopRecordingCommand}"
                        Width="120" />
            </StackPanel>

            <TextBlock Text="{Binding RecordingStatus}"
                       Foreground="Red"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       Margin="2,12,2,0" />

            <TextBlock Text="入力レベル" Margin="2,10,2,0" />
            <ProgressBar Minimum="0"
                         Maximum="1"
                         Value="{Binding CurrentVolume}"
                         Height="18"
                         Margin="2,4,2,0" />
        </StackPanel>
    </Grid>
</UserControl>
